This example tests the Hexagon optimized audio upsampling routines 
found in "codec_upsamp_filt.h".  It runs against a sample set of 
data and compares the output against precomputed values.

To run:

make clean
make sim

The simulation will print "*** PASS" if the test passed.


