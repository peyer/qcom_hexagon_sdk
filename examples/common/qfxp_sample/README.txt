The qdebug_sample application demonstrates how to build & link an application that uses
qdebug API's. It can be built and run as a Hexagon simulator executable, or as an Android
application and DSP shared library.

To build and run in simulation, run simulator.run.cmd

To build and run in Android, run target.run.cmd
